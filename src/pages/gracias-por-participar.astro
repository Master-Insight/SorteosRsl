---
import Layout from "../layouts/Layout.astro";

// Obtener los parámetros de la URL
const url = new URL(Astro.request.url);
const Nombre = url.searchParams.get("Nombre") || "Participante";
const Apellido = url.searchParams.get("Apellido") || "";
---

<Layout>
  <main class="min-h-screen bg-green-50 flex items-center justify-center p-4">
    <div
      class="max-w-xl w-full bg-white rounded-xl shadow-xl p-6 text-gray-800 text-center"
    >
      <h1 class="text-3xl md:text-4xl font-bold text-green-700 mb-4">
        🎉 ¡Gracias por registrarte, {Nombre}
        {Apellido}!
      </h1>

      <p class="text-lg md:text-xl mb-6">
        Tu participación ha sido <strong>confirmada con éxito</strong> en el sorteo.
      </p>

      <p class="text-base text-gray-600 mb-6">
        Ya estás dentro. ¡Mucha suerte! Te contactaremos si resultás ganador/a.
      </p>

      <div class="flex justify-center">
        <a
          href="/"
          class="inline-flex items-center justify-center bg-gray-700 hover:bg-gray-800 text-white font-semibold py-3 px-5 rounded-lg shadow transition duration-200"
        >
          Volver al inicio
        </a>
      </div>
    </div>
  </main>
</Layout>
