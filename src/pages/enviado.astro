---
import Layout from "../layouts/Layout.astro";

// Obtener los parámetros de la URL
const url = new URL(Astro.request.url);
const Nombre = url.searchParams.get("Nombre") || "Participante";
const Apellido = url.searchParams.get("Apellido") || "";
const Telefono = url.searchParams.get("Telefono") || "";
const linkPago = "https://mpago.la/1TFD7F1";
---

<Layout>
  <main
    class="min-h-screen bg-black text-white flex items-center justify-center p-4"
  >
    <div class="max-w-xl w-full bg-white rounded-lg shadow-lg p-6 text-black">
      <h1 class="text-2xl md:text-3xl font-bold text-center mb-6">
        ¡Gracias por participar, {Nombre}
        {Apellido}!
      </h1>
      <div class="space-y-4">
        <p class="text-base md:text-lg text-center">
          Para completar tu participación, haz clic en el enlace de pago de
          Mercado Pago:
        </p>
        <!-- Botón de pago con Mercado Pago -->
        <div class="flex justify-center">
          <!-- <a
            href={linkPago}
            target="_blank"
            class="w-full md:w-auto h-12 flex items-center justify-center bg-blue-600 text-white rounded-md text-center hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
          >
            Pagar con Mercado Pago
          </a> -->
          <a href={linkPago} target="_blank" class="inline-block">
            <img
              src="/images/mercado-pago-logo.svg"
              alt="Pagar con Mercado Pago"
              class="w-48 h-auto m-6"
            />
          </a>
        </div>
        <p class="text-base md:text-lg text-center">
          Una vez realizado el pago, tu participación se confirmará
          automáticamente.
        </p>
        <div class="flex justify-center">
          <a
            href="/"
            class="w-full md:w-auto h-12 flex items-center justify-center bg-gray-600 text-white rounded-md text-center hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-gray-500 p-3"
          >
            Volver al inicio
          </a>
        </div>
      </div>
    </div>
  </main>
</Layout>
